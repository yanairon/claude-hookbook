{
  "slug": "bell-permission-prompt",
  "name": "Terminal bell on permission prompt",
  "oneLine": "Rings the terminal bell when Claude asks for permission, via the terminalSequence output field.",
  "description": "The simplest hook in the library: a one-line command that rings the terminal bell on permission prompts. No script file, no jq, nothing to install. Hooks run without a controlling terminal, so a plain printf of the BEL character never reaches your terminal - it lands in the debug log. Instead the hook prints a tiny JSON object with the terminalSequence field (bare BEL is on the allowlist), and Claude Code emits it through its own terminal write path. Interactive sessions only: in -p mode or the Agent SDK the field is ignored.",
  "category": [
    "notifications"
  ],
  "tags": [
    "zero-dep"
  ],
  "event": "Notification",
  "matcher": "permission_prompt",
  "handlerType": "command",
  "command": "printf '{\"terminalSequence\":\"\\u0007\"}'",
  "timeout": 5,
  "scope": "user",
  "platforms": [
    "macos",
    "linux",
    "windows"
  ],
  "requires": [],
  "risk": "low",
  "failureMode": "observe",
  "installSteps": [
    "Add the config fragment to ~/.claude/settings.json. That is the whole install."
  ],
  "testSteps": [
    "Positive: trigger a permission prompt in an interactive session - the terminal bell rings (check your emulator's bell settings if silent).",
    "Negative: regular output should not ring.",
    "Note: in non-interactive (-p) runs nothing rings - Claude Code ignores terminalSequence there."
  ],
  "sources": [
    {
      "title": "Claude Code hooks reference",
      "url": "https://code.claude.com/docs/en/hooks",
      "type": "official-docs",
      "checkedAt": "2026-09-10"
    }
  ],
  "verifiedAgainst": "Claude Code hooks reference, event/matcher schema checked 2026-09-10",
  "testStatus": "needs-manual-check",
  "seoTitle": "Terminal bell on permission prompt - Claude Code hook",
  "seoDescription": "Zero-dependency hook that rings the terminal bell on permission prompts.",
  "relatedHooks": [
    "notify-input-needed"
  ]
}