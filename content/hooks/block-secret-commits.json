{
  "slug": "block-secret-commits",
  "name": "Block commits that contain secrets",
  "oneLine": "Intercepts git commit commands and runs gitleaks on staged changes first.",
  "description": "Fires before Bash calls. Only commands that actually invoke git commit are intercepted; the hook runs gitleaks over staged changes and denies the commit when findings appear, so a secret never reaches history. Every other command passes through untouched.",
  "category": [
    "safety"
  ],
  "tags": [
    "secrets",
    "git",
    "gitleaks"
  ],
  "event": "PreToolUse",
  "matcher": "Bash",
  "handlerType": "command",
  "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/block-secret-commits.sh",
  "scriptPath": ".claude/hooks/block-secret-commits.sh",
  "scriptBody": "#!/bin/bash\n# block-secret-commits: scan staged changes before any git commit.\nset -u\ncmd=$(jq -r '.tool_input.command // empty')\n[ -z \"$cmd\" ] && exit 0\ncase \"$cmd\" in\n  *\"git commit\"*) ;;\n  *) exit 0 ;;\nesac\ncommand -v gitleaks >/dev/null 2>&1 || exit 0\ncd \"${CLAUDE_PROJECT_DIR:-$PWD}\" || exit 0\nif ! gitleaks git --staged --no-banner . >/dev/null 2>&1; then\n  jq -n '{\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      permissionDecision: \"deny\",\n      permissionDecisionReason: \"gitleaks found a possible secret in staged changes. Unstage or mask it, then commit again.\"\n    }\n  }'\n  exit 0\nfi\nexit 0\n",
  "timeout": 60,
  "scope": "project",
  "platforms": [
    "macos",
    "linux"
  ],
  "requires": [
    "jq",
    "gitleaks",
    "git"
  ],
  "risk": "blocking",
  "failureMode": "block",
  "recoveryNote": "The commit is only denied, nothing is lost. Unstage the flagged file or mask the secret, then ask Claude to commit again. If gitleaks is not installed the hook silently stands down - treat it as required, not optional.",
  "installSteps": [
    "Install gitleaks and jq.",
    "Save the script to .claude/hooks/block-secret-commits.sh and chmod +x it.",
    "Add the config fragment to .claude/settings.json."
  ],
  "testSteps": [
    "Positive: stage a file with a fake AWS key and ask Claude to commit - the commit must be denied.",
    "Negative: a clean staged change commits normally.",
    "Negative: 'git status' and 'git diff' are not intercepted."
  ],
  "sources": [
    {
      "title": "Claude Code hooks reference",
      "url": "https://code.claude.com/docs/en/hooks",
      "type": "official-docs",
      "checkedAt": "2026-09-10"
    },
    {
      "title": "gitleaks",
      "url": "https://github.com/gitleaks/gitleaks",
      "type": "community",
      "checkedAt": "2026-09-10"
    }
  ],
  "verifiedAgainst": "Claude Code hooks reference, event/matcher schema checked 2026-09-10",
  "testStatus": "needs-manual-check",
  "seoTitle": "Block commits that contain secrets - Claude Code hook",
  "seoDescription": "A PreToolUse hook that runs gitleaks on staged changes before any git commit.",
  "relatedHooks": [
    "secret-scan-changed-files",
    "protect-env-secrets"
  ]
}
