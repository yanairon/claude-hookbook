{
  "slug": "protect-env-secrets",
  "name": "Protect .env and secret files",
  "oneLine": "Blocks Claude from writing to .env files, keys, and credential files - unless you say otherwise.",
  "description": "Fires before Edit/Write tool calls. The script checks the target file's name against a protected list (.env, .env.*, *.pem, *.key, anything with 'credentials' or 'secrets' in the name) and returns a permissionDecision of deny with a clear reason. Everything else passes through to the normal permission flow. Official-guide pattern, updated to the current JSON decision output.",
  "category": [
    "safety"
  ],
  "tags": [
    "secrets"
  ],
  "event": "PreToolUse",
  "matcher": "Edit|Write",
  "handlerType": "command",
  "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/protect-env-secrets.sh",
  "scriptPath": ".claude/hooks/protect-env-secrets.sh",
  "scriptBody": "#!/bin/bash\n# protect-env-secrets: deny writes to secret-bearing files.\nset -u\nfile=$(jq -r '.tool_input.file_path // empty')\n[ -z \"$file\" ] && exit 0\nbase=$(basename \"$file\")\nprotected=0\ncase \"$base\" in\n  .env|.env.*|*.pem|*.key|*.p12|*.pfx) protected=1 ;;\n  *credential*|*secret*) protected=1 ;;\nesac\nif [ \"$protected\" = \"1\" ]; then\n  jq -n --arg f \"$file\" '{\n    hookSpecificOutput: {\n      hookEventName: \"PreToolUse\",\n      permissionDecision: \"deny\",\n      permissionDecisionReason: (\"Writes to secret-bearing files are blocked by the protect-env-secrets hook (\" + $f + \"). Edit it yourself, or temporarily disable the hook.\")\n    }\n  }'\n  exit 0\nfi\nexit 0\n",
  "timeout": 10,
  "scope": "either",
  "platforms": [
    "macos",
    "linux"
  ],
  "requires": [
    "jq"
  ],
  "risk": "blocking",
  "failureMode": "block",
  "recoveryNote": "When Claude is blocked it sees the deny reason and can tell you. To let a specific edit through, make the edit yourself, or remove/disable the hook entry in settings.json for that session.",
  "installSteps": [
    "Save the script to .claude/hooks/protect-env-secrets.sh and chmod +x it.",
    "Add the config fragment to .claude/settings.json (project scope) or ~/.claude/settings.json (everywhere)."
  ],
  "testSteps": [
    "Positive: ask Claude to 'add a line to .env' - the write must be denied with the hook's reason visible.",
    "Negative: ask Claude to edit .env.example... note: this one IS caught too (matches .env.*). Rename it or narrow the case list if that bothers you.",
    "Negative: ask Claude to edit src/index.ts - normal flow, no denial."
  ],
  "sources": [
    {
      "title": "Claude Code hooks reference",
      "url": "https://code.claude.com/docs/en/hooks",
      "type": "official-docs",
      "checkedAt": "2026-09-10"
    },
    {
      "title": "Claude Code hooks guide",
      "url": "https://code.claude.com/docs/en/hooks-guide",
      "type": "official-docs",
      "checkedAt": "2026-09-10"
    }
  ],
  "verifiedAgainst": "Claude Code hooks reference, event/matcher schema checked 2026-09-10",
  "testStatus": "needs-manual-check",
  "seoTitle": "Protect .env and secret files - Claude Code hook",
  "seoDescription": "Block Claude Code from writing to .env files, keys, and credential files with a PreToolUse hook.",
  "relatedHooks": [
    "block-destructive-commands",
    "secret-scan-changed-files"
  ]
}
