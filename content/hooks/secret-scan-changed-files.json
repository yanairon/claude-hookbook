{
  "slug": "secret-scan-changed-files",
  "name": "Secret scan on changed files",
  "oneLine": "Runs gitleaks on every file Claude edits and warns when something looks like a secret.",
  "description": "Fires after Edit/Write. The script pipes the changed file through gitleaks stdin mode and, on findings, exits 2 so Claude sees the warning after the edit and can offer to fix it. Starts warn-only on purpose: blocking on every edit gets noisy fast.",
  "category": [
    "safety"
  ],
  "tags": [
    "secrets",
    "gitleaks"
  ],
  "event": "PostToolUse",
  "matcher": "Edit|Write",
  "handlerType": "command",
  "command": "${CLAUDE_PROJECT_DIR}/.claude/hooks/secret-scan-changed-files.sh",
  "scriptPath": ".claude/hooks/secret-scan-changed-files.sh",
  "scriptBody": "#!/bin/bash\n# secret-scan-changed-files: warn when an edit introduces something secret-shaped.\nset -u\nfile=$(jq -r '.tool_input.file_path // empty')\n[ -z \"$file\" ] && exit 0\n[ -f \"$file\" ] || exit 0\ncommand -v gitleaks >/dev/null 2>&1 || exit 0\nif ! gitleaks stdin --no-banner < \"$file\" >/dev/null 2>&1; then\n  echo \"gitleaks found a possible secret in $file - review before committing.\" >&2\n  exit 2\nfi\nexit 0\n",
  "timeout": 30,
  "scope": "project",
  "platforms": [
    "macos",
    "linux"
  ],
  "requires": [
    "jq",
    "gitleaks"
  ],
  "risk": "review",
  "failureMode": "warn",
  "installSteps": [
    "Install gitleaks (brew install gitleaks / see gitleaks.io) and jq.",
    "Save the script to .claude/hooks/secret-scan-changed-files.sh and chmod +x it.",
    "Add the config fragment to .claude/settings.json."
  ],
  "testSteps": [
    "Positive: have Claude write a file containing a fake AWS key (AKIA...) - Claude should see the gitleaks warning.",
    "Negative: a file with the word 'secret' in a comment but no real pattern should pass."
  ],
  "sources": [
    {
      "title": "Claude Code hooks reference",
      "url": "https://code.claude.com/docs/en/hooks",
      "type": "official-docs",
      "checkedAt": "2026-09-10"
    },
    {
      "title": "gitleaks",
      "url": "https://github.com/gitleaks/gitleaks",
      "type": "community",
      "checkedAt": "2026-09-10"
    }
  ],
  "verifiedAgainst": "Claude Code hooks reference, event/matcher schema checked 2026-09-10",
  "testStatus": "needs-manual-check",
  "seoTitle": "Secret scan on changed files - Claude Code hook",
  "seoDescription": "Scan every file Claude edits with gitleaks and warn on possible secrets.",
  "relatedHooks": [
    "protect-env-secrets",
    "block-secret-commits"
  ]
}
